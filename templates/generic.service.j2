[Unit]
Requires=network-online.target
After=network-online.target

[Service]
EnvironmentFile=-{{ config_folder }}/{{ app }}.env
User={{ user }}
Group={{ user }}
ExecStart={{ bin_folder }}/{{ app }} agent -config-dir={{ config_folder }}/{{ app }}.d
ExecReload=/bin/kill --signal HUP $MAINPID
KillMode=process
KillSignal=SIGTERM
Restart=on-failure
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
