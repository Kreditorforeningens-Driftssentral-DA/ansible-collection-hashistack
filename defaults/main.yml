# DEFAULTS
---
# Permissions
app_user: root

# Folders
folder_binary: /usr/local/bin
folder_config: /etc/opt
folder_data: /var/opt
folder_service: /etc/systemd/system
folder_cni: /opt/cni

# Filename(s)
manage_config_file: "{{ app_name }}"

# Asserts
allowed_applications:
- boundary
- consul
- nomad
- packer
- terraform
- vault
- waypoint
