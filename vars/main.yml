# VARIABLES
---
#hashistack:
#  name: terraform
#  version: '1.2.2'     # triggers _app_install
#  config: {}           # triggers _app_configuration
#  service: false       # triggers _app_service
#  cni_version: '1.1.1' # triggers _cni_install
#  force: false         # forces reinstallation
